# 🛠 Локальная Разработка - Boardly

## Быстрый Старт

### 1. Установка зависимостей
```bash
npm install
```

### 2. Настройка окружения

У вас уже есть `.env.local` с настройками. Для работы Prisma CLI создайте копию:
```bash
cp .env.local .env
```

> ⚠️ **Важно**: `.env` и `.env.local` уже в `.gitignore` и не попадут в git

### 3. Настройка базы данных

Генерация Prisma Client:
```bash
npm run db:generate
```

Проверка статуса миграций:
```bash
npx prisma migrate status
```

### 4. Запуск Dev Серверов

**Вариант 1: Два терминала (рекомендуется)**

Терминал 1 - Socket.IO сервер:
```bash
npm run socket:dev
```

Терминал 2 - Next.js:
```bash
npm run dev
```

**Вариант 2: Один терминал (будет добавлен concurrently)**
```bash
npm run dev:all
```

## 📍 URLs

- **Frontend**: http://localhost:3000
- **Socket.IO**: http://localhost:3001
- **Prisma Studio** (БД GUI): `npm run db:studio` → http://localhost:5555

## 🔧 Полезные Команды

### Database
```bash
# Просмотр БД через GUI
npm run db:studio

# Применить изменения схемы без миграций (dev only)
npm run db:push

# Создать новую миграцию
npx prisma migrate dev --name your_migration_name

# Обновить Prisma Client после изменения schema.prisma
npm run db:generate

# Сбросить БД (удалит все данные!)
npx prisma migrate reset
```

### Development
```bash
# Линтинг
npm run lint

# Билд продакшн версии (для проверки)
npm run build

# Запуск продакшн версии локально
npm run build && npm start
```

## 📁 Структура Environment Variables

### Обязательные для локальной разработки:
```env
NODE_ENV=development
DATABASE_URL=postgresql://...
JWT_SECRET=...
NEXTAUTH_SECRET=...
NEXTAUTH_URL=http://localhost:3000
PORT=3001
HOSTNAME=0.0.0.0
CORS_ORIGIN=http://localhost:3000,http://127.0.0.1:3000
```

### Опциональные:
```env
# Email (для тестирования регистрации)
RESEND_API_KEY=...
EMAIL_FROM="Boardly <noreply@boardly.online>"

# OAuth (для социального входа)
GOOGLE_CLIENT_ID=...
GOOGLE_CLIENT_SECRET=...

# Мониторинг (можно отключить локально)
NEXT_PUBLIC_SENTRY_DSN=...
```

## 🐛 Решение Проблем

### Socket.IO не подключается
- Убедитесь, что Socket.IO сервер запущен на порту 3001
- Проверьте переменную `CORS_ORIGIN` в `.env.local`
- Проверьте, что нет конфликта портов: `lsof -i :3001` (Mac/Linux) или `netstat -ano | findstr :3001` (Windows)

### Ошибки БД
- Проверьте, что `DATABASE_URL` валидна и БД доступна
- Запустите `npm run db:generate` для обновления Prisma Client
- Проверьте статус миграций: `npx prisma migrate status`

### Next.js не запускается
- Удалите `.next` папку: `rm -rf .next` или `rmdir /s /q .next`
- Очистите node_modules: `rm -rf node_modules && npm install`
- Проверьте, что порт 3000 свободен

## 🔄 Workflow для Разработки

1. **Создайте новую ветку**
   ```bash
   git checkout -b feature/your-feature-name
   ```

2. **Запустите dev серверы**
   ```bash
   npm run socket:dev  # В одном терминале
   npm run dev         # В другом терминале
   ```

3. **Разрабатывайте с hot reload**
   - Next.js автоматически перезагружается при изменении файлов
   - Socket.IO сервер нужно перезапускать вручную

4. **Тестируйте изменения**
   - Откройте http://localhost:3000
   - Проверьте в разных браузерах
   - Проверьте мобильную версию (DevTools)

5. **Коммит и push**
   ```bash
   git add .
   git commit -m "feat: your feature description"
   git push origin feature/your-feature-name
   ```

## 🎨 Рекомендации для Полировки

### UI/UX
- [ ] Проверить адаптивность на всех разрешениях
- [ ] Улучшить анимации и переходы
- [ ] Добавить loading states для всех async операций
- [ ] Улучшить error messages
- [ ] Добавить tooltips для сложных элементов

### Performance
- [ ] Оптимизировать изображения
- [ ] Добавить lazy loading для компонентов
- [ ] Проверить bundle size: `npm run build && npx @next/bundle-analyzer`
- [ ] Добавить caching стратегию

### Code Quality
- [ ] Добавить TypeScript strict mode
- [ ] Написать тесты для критичных функций
- [ ] Добавить JSDoc комментарии
- [ ] Убрать неиспользуемый код

### Безопасность
- [ ] Проверить rate limiting
- [ ] Добавить CSRF protection для всех форм
- [ ] Валидация всех пользовательских input
- [ ] Проверить SQL injection защиту (Prisma делает это автоматически)

## 📚 Дополнительные Ресурсы

- [Next.js Documentation](https://nextjs.org/docs)
- [Prisma Documentation](https://www.prisma.io/docs)
- [Socket.IO Documentation](https://socket.io/docs/v4/)
- [Tailwind CSS Documentation](https://tailwindcss.com/docs)
