# 🚀 Быстрый Старт - Локальная Разработка

## ✅ Готово к использованию!

Ваш проект настроен и готов к локальной разработке.

## 📋 Что уже сделано

- ✅ Зависимости проверены и обновлены
- ✅ `.env.local` настроен
- ✅ База данных Supabase подключена
- ✅ Prisma Client сгенерирован
- ✅ Миграции применены

## 🎮 Запуск проекта

### Вариант 1: Один терминал (простой)

```bash
npm run dev:all
```

Это запустит оба сервера одновременно:
- 🌐 Next.js: http://localhost:3000
- 🔌 Socket.IO: http://localhost:3001

### Вариант 2: Два терминала (больше контроля)

**Терминал 1:**
```bash
npm run socket:dev
```

**Терминал 2:**
```bash
npm run dev
```

## 🛠 Полезные команды

```bash
# База данных
npm run db:studio          # GUI для просмотра БД (http://localhost:5555)
npm run db:generate        # Обновить Prisma Client после изменения schema
npm run db:push            # Применить изменения schema к БД (dev only)

# Разработка
npm run lint               # Проверка кода
npm run build              # Тестовая сборка продакшн версии

# Проверка здоровья проекта
bash health-check.sh       # Запустить проверку проекта
```

## 📁 Важные файлы

- **DEV_SETUP.md** - Детальная документация по разработке
- **POLISH_CHECKLIST.md** - Чек-лист для полировки перед продакшном
- **SENTRY_FIX.md** - Как исправить Sentry warnings (опционально)

## 🎯 Следующие шаги

1. **Откройте проект**: http://localhost:3000
2. **Протестируйте основной функционал**:
   - Регистрация/вход
   - Создание лобби
   - Игра в Yahtzee
3. **Начните полировку**: Используйте `POLISH_CHECKLIST.md`

## 🐛 Устранение проблем

### Порт занят
```bash
# Windows
netstat -ano | findstr :3000
netstat -ano | findstr :3001

# Убить процесс
taskkill /PID <PID> /F
```

### Ошибки БД
```bash
# Пересоздать Prisma Client
npm run db:generate

# Проверить подключение
npx prisma db execute --stdin <<< "SELECT 1"
```

### Ошибки зависимостей
```bash
# Переустановить зависимости
rm -rf node_modules package-lock.json
npm install
```

### Sentry warnings (не критично)
- Добавьте в `.env.local`: `SENTRY_SUPPRESS_GLOBAL_ERROR_HANDLER_FILE_WARNING=1`
- Или закомментируйте `NEXT_PUBLIC_SENTRY_DSN` если не нужен локально
- Подробности: `SENTRY_FIX.md`

## 💡 Советы

- **Hot Reload**: Next.js автоматически перезагружается при изменениях
- **Socket.IO**: Нужно перезапускать вручную при изменении `socket-server.ts`
- **БД changes**: После изменения `schema.prisma` запустите `npm run db:push` или создайте миграцию
- **Git**: `.env` и `.env.local` уже в `.gitignore` - ваши секреты в безопасности

## 📊 Структура проекта

```
Boardly/
├── app/              # Next.js App Router страницы
├── components/       # React компоненты
├── lib/              # Утилиты и логика
├── prisma/           # Database schema и миграции
├── public/           # Статические файлы (звуки, изображения)
├── types/            # TypeScript types
└── socket-server.ts  # WebSocket сервер
```

## 🎨 Готовы к полировке?

Откройте **POLISH_CHECKLIST.md** и начните систематически улучшать проект!

---

**Нужна помощь?** Проверьте DEV_SETUP.md или документацию в docs/
